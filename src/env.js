(function (window) {
  window.__env = window.__env || {};

  // couterAPI KEY
  window.__env.counterKey = "1ccb732e-b55a-4404-ad3f-0f99c02fe44e";
  window.__env.counterNamespace = "project-purple-cow";

  // Whether or not to enable debug mode
  // Setting this to false will disable console output
  window.__env.enableDebug = true;
})(this);
